spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inventra
    username: postgres
    password: yuviR123#

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
jwt:
  secret: InventraSecretKeyInventraSecretKeyInventraSecretKey123
  expiration: 86400000
